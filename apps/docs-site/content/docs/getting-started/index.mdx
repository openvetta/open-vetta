---
title: 快速开始
description: 安装 Vetta、完成首次引导、配置模型，并为第一个可验证任务准备工作区。
---

完成本页后，你会得到一个已登录、有可用模型、能够创建本地项目的 Vetta 桌面工作区。整个过程通常只需一次；之后可以直接从项目或「对话」开始任务。

<figure className="docs-media-frame docs-media-frame--wide">
  <img src="/images/product/workspace.png" alt="完成首次设置后的 Vetta 主工作区" width="1920" height="1280" />
  <figcaption>准备完成后，侧栏管理项目和能力，中间区域创建会话并提交任务。</figcaption>
</figure>

## 准备工作

- 一台支持 Vetta 桌面客户端的计算机（Windows / macOS / Linux，以[下载页](https://openvetta.com/download)实际发布包为准）。
- 可用的 Vetta 账号（桌面端通过浏览器 **授权登录** 完成身份认证）。
- 至少一个可用的模型服务：组织提供的远程模型，或你在「设置 → 模型配置」中自行添加的服务商与密钥。

## 安装与首次启动

<Steps>
  <Step>
    ### 下载并安装

    从 [Vetta 下载页](https://openvetta.com/download)获取适合当前操作系统和处理器架构的安装包，完成安装后启动 Vetta。下载页没有列出的平台或架构表示当前没有可用制品，不要使用其他平台安装包替代。
  </Step>
  <Step>
    ### 完成首次引导

    首次启动会进入引导流程，主要包括：

    1. **语言与外观**：选择界面语言（跟随系统 / 中文 / English）和外观偏好。
    2. **系统权限**（仅 macOS）：按提示完成应用所需权限。
    3. **登录**（未登录时）：点击 **授权登录**，在浏览器完成 OAuth 后返回应用。
    4. **欢迎页**：引导结束进入主界面。

    之后可在「设置 → 通用设置」中通过 **启动 App 引导** 重新打开该流程。
  </Step>
  <Step>
    ### 确认登录状态

    侧栏账户区域应显示已登录。企业用户使用组织提供的登录方式；授权链接失效时，在登录弹层中重新授权。
  </Step>
</Steps>

## 配置模型

打开 **设置 → 模型配置**（侧栏也可进入模型相关入口）：

1. 在 **预设服务商** 中选择已支持的服务商（如 Claude、OpenAI、DeepSeek、Z.ai (GLM)、Kimi、Grok、Qwen、Gemini 等），填写 API Key。
2. 使用 **从接口拉取** 获取模型列表并勾选添加，或手动 **添加模型**。
3. 将常用模型 **设为默认模型**。
4. 可选：调整全局 **思考** 档位（关闭 / 极低 / 低 / 中 / 高 / 极高）；模型不支持时会自动降级。

没有预设时，使用 **添加服务商** 配置自定义名称、API 类型、Base URL 与 API Key。

<Callout title="密钥安全" type="warn">
  访问密钥只应填写在凭据配置界面。支持 `sk-...`、`env:MY_API_KEY` 等形式。不要把密钥写入提示词、项目文件或问题报告。配置落在本机 `~/.vetta/agent/models.json`。
</Callout>

## 主界面入口（侧栏）

侧栏主区常见入口：

| 入口 | 用途 |
| --- | --- |
| 新会话 / 项目区 | 在工作目录中创建会话并执行任务 |
| 自动化 | 按计划单次或周期执行任务 |
| 知识库 | 导入并整理可长期检索的资料（界面可能带 BETA 标识） |
| 能力 | 技能、场景、MCP、插件、套装 |

**更多** 菜单中通常还有：

| 入口 | 用途 |
| --- | --- |
| 批量任务 | 同一提示处理多个文件夹 |
| 模型设置 | 跳转到设置中的模型配置 |
| Agent 设置 | 跳转到 Agent 配置 |
| 外观 | 跳转到外观设置 |

完整设置（Claw、知识库后台、应用环境、消息推送、Vetta Vivi 等）从侧栏 **设置** 进入。场景类型可在能力页管理；另有路由 `/scenes` 的场景专页，但不一定作为侧栏一级入口展示。

## 确认准备完成

开始第一个任务前，逐项确认：

- 账户区域显示已登录，或当前部署允许未登录使用。
- 「设置 → 模型配置」至少有一个模型，并已设为默认模型。
- 新建会话页能够选择该模型，没有显示凭证或连接错误。
- 需要处理本地文件时，已经创建或打开正确的项目目录。
- 已理解新会话的执行模式是「沙盒受限」还是「完全访问」。

如果模型测试失败，先处理配置问题，不要在任务中反复重试。参见[配置模型](/product/models/)和[故障排查](/troubleshooting/)。

## 下一步

<Cards>
  <Card title="运行第一个任务" href="/getting-started/first-task/" description="选择工作目录、编写目标并核对执行结果。" />
  <Card title="配置模型" href="/product/models/" description="预设服务商、自定义服务商与验证方式。" />
  <Card title="使用指南概览" href="/product/overview/" description="按任务浏览连接、执行与扩展能力。" />
</Cards>
