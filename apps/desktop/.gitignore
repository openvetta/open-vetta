# .env.* 一律不纳入版本管理（根 .gitignore 已全部忽略）。
# 曾经把 .env.production / .env.test 加进白名单，但那是本团队的发布约定，
# 不该强加给下游——生产端点、更新源、租户各家不同。
# 构建配置改由 CI 变量或本地 .env.<mode> 提供，示例见 docs/desktop/build-modes.md。

# DMG 背景图由 scripts/generate-dmg-background.js 在 mac 打包时生成
/build/background.png
/build/background@2x.png

# 内置插件 zip 解压后的开发 staging
/.artifacts/

# appshot-helper 编译产物（scripts/build-appshot-helper.js 生成）
/resources/appshot/

# WebdriverIO Electron E2E runtime data
/.wdio-electron-user-data/
