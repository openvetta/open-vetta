---
title: 运行第一个完整任务
description: 在真实工作区中创建会话、提供上下文、检查执行过程，并用实际文件和验证结果完成验收。
---

本页用一个小型文档任务走完 Vetta 的标准闭环：选择工作区、提交可执行目标、处理权限请求、检查产物并继续修正。第一次不要选择大规模迁移或不可逆操作。

## 本次任务的结果

假设项目中已有 `README.md` 和 `package.json`，让 Vetta 补充一份准确的本地开发说明。完成后应看到：

- 项目目录中新增或更新 `docs/local-development.md`。
- 文档中的安装、检查和测试命令来自当前项目事实，而不是模型猜测。
- Agent 实际运行至少一个无副作用的检查命令，并在最终回复中报告结果。
- 除目标文档外没有意外修改。

## 创建工作区和会话

<Steps>
  <Step>
    ### 打开项目

    在侧栏选择 **打开项目**，选中准备好的本地目录。项目会成为本次任务的文件和工具边界。
  </Step>
  <Step>
    ### 新建会话

    在项目下选择 **新会话**，确认模型正确。首次试跑优先选择 **沙盒受限**；平台不可用或任务确实需要跨目录访问时，再评估完全访问。
  </Step>
  <Step>
    ### 引用关键文件

    在输入区使用 `@` 引用 `README.md` 和 `package.json`。直接引用比只说“参考项目配置”更明确，也能减少无关文件读取。
  </Step>
</Steps>

## 提交可验收的任务

把目标、范围、约束和验证写在同一条消息中：

```text
阅读 @README.md 和 @package.json，为这个项目补充 docs/local-development.md。

要求：
1. 说明环境要求、安装、启动、快速检查和定向测试；
2. 命令必须来自当前仓库脚本，不要虚构；
3. 只修改目标文档，不修改 package.json 或源码；
4. 完成后实际运行无副作用的快速检查，并报告命令和结果；
5. 如果信息不足，先说明缺口，不要猜测。
```

这类任务足够小，便于检查 Agent 是否正确理解文件、遵守范围并执行验证。

## 执行时看什么

| 信号 | 正常表现 | 需要介入 |
| --- | --- | --- |
| 待办 | 读取事实、起草、检查、验证逐步推进 | 长时间停在同一步或目标发生漂移 |
| 工具调用 | 读取指定文件，写入目标文档，运行检查 | 访问无关目录或准备执行高影响命令 |
| 权限请求 | 动作与当前任务直接相关 | 路径、命令或网络目的地无法解释 |
| 后台任务 | 检查命令运行后正常结束 | 持续运行、失败或与任务无关 |

权限请求提供 **允许本次**、**本会话不再询问** 和 **拒绝**。第一次使用优先逐次允许；不确定时拒绝并在消息中补充边界。

## 验收真实结果

任务结束后，不只阅读最终回复：

<Steps>
  <Step>
    ### 打开目标文件

    在活动面板的文件标签中打开 `docs/local-development.md`，确认内容可读且命令与项目脚本一致。
  </Step>
  <Step>
    ### 检查修改范围

    查看项目文件或自己的版本控制状态，确认只有预期文档发生变化。
  </Step>
  <Step>
    ### 核对验证证据

    展开对应命令工具卡片，确认命令确实执行、退出状态正常，而不是只在回复中声称“已通过”。
  </Step>
  <Step>
    ### 阅读风险说明

    确认最终回复列出未运行的检查、信息缺口或需要人工确认的事项。
  </Step>
</Steps>

## 结果不符合时继续

保持在原会话中，引用具体问题并缩小修正范围：

```text
开发命令准确，但测试章节把全量测试写成默认步骤。
只修改 docs/local-development.md：把默认流程改成定向测试，并保留全量测试作为发布前选项。
修改后重新核对 package.json，不需要运行源码测试。
```

同一目标的修正继续使用原会话；要比较另一种完整方案时使用分叉；开始无关目标时新建会话。

## 下一步

<Cards>
  <Card title="理解核心工作流" href="/core/overview/" description="建立工作区、会话、执行和结果的完整心智。" />
  <Card title="上下文、工具与权限" href="/core/context-tools-and-permissions/" description="更精确地提供材料并控制动作边界。" />
  <Card title="进度、结果与恢复" href="/core/progress-results-and-recovery/" description="检查长任务、后台工作和失败恢复。" />
</Cards>
