# @vetta/action-rpc

Localhost HTTP JSON RPC transport for Vetta app actions.

This package owns transport, protocol, server, and client helpers only. It does
not define desktop-app business actions.
