# 仓库根作为 Vercel 上传根时生效。
#
# 为什么不能只传 packages/site：site 依赖 @vetta/{ui,theme-ui,theme-sdk,markdown}
# 四个 workspace 包，单独上传时 package.json 里的 "workspace:*" npm/bun 解析不了
# （报 Unsupported URL Type "workspace:"）。依赖必须在仓库根安装，所以要传仓库。
#
# 排除策略：按「产物目录名」排，不带路径前缀的模式在任意层级生效，
# 新增的包会自动覆盖到，不需要逐个补路径。
# 不要排除 workspace 成员目录本身——根 package.json 的 workspaces 里列到的包若缺失，
# 仓库根的 bun install 会直接失败。
#
# 注意：Vercel 只读上传根目录下的这一份，packages/site/.vercelignore 不再生效。

# ── 依赖与构建产物（任意层级）──
node_modules
dist
.next
out
release
.desktop-dev
.turbo
.cache
.vercel
*.log
*.tsbuildinfo
*.bun-build

# ── 环境文件不上传，线上值走 Vercel 环境变量 ──
.env
.env.*
!.env.example

# ── 与前端构建无关的顶层目录（都不是 workspace 成员，可整个排掉）──
.git
.github
releases
deploy
docs
packages/api
packages/im-gateway
