# 仓库根作为 Vercel 上传根时生效。
#
# docs-site 的安装与构建复用仓库根 lockfile、Bun 和 Turbo 入口，因此 Vercel
# 仍需上传 Monorepo 的工作区元数据；Root Directory 设为 apps/docs-site。
#
# 排除策略：按「产物目录名」排，不带路径前缀的模式在任意层级生效，
# 新增的包会自动覆盖到，不需要逐个补路径。
# 不要排除 workspace 成员目录本身——根 package.json 的 workspaces 里列到的包若缺失，
# 仓库根的 bun install 会直接失败。
#
# 注意：Vercel 使用上传根目录下的这一份规则。

# ── 依赖与构建产物（任意层级）──
node_modules
dist
.next
out
release
.desktop-dev
.turbo
.cache
.vercel
*.log
*.tsbuildinfo
*.bun-build

# ── 环境文件不上传，线上值走 Vercel 环境变量 ──
.env
.env.*
!.env.example

# ── 与前端构建无关的顶层目录（都不是 workspace 成员，可整个排掉）──
# 顶层路径必须以 `/` 锚定；未锚定的 `docs` 也会误删
# `apps/docs-site/content/docs`，让 Fumadocs 在部署时得到空内容源。
/.git
/.github
/releases
/deploy
/docs
/apps/api
/apps/im-gateway
