# SSH 远程项目与执行边界

## 背景

项目身份此前等同于一个本地绝对路径：`ProjectEntry` 只有 `path`，`sameProjectPath()` 把路径归一化后直接比较，会话 cwd、活动标签 keying 和文件树授权根全部建立在「cwd 字符串唯一且指向本机」这个假设上。用户要在远程主机上开发时，只能把仓库同步到本地，或者完全绕开 Vetta。

Desktop 的 UI 文件层集中在 `filesystem-service.ts`，但它直接调用 `node:fs`，没有后端抽象。Agent 侧相反：`CodingAgentToolEnvironmentFactory` 与各工具的 `*Operations` 端口已经是干净的注入点，ADR-0075 也明确写了「后续平台使用独立 Runtime 包实现同一协议」。

调研了三种业界形态：

- **远端完整 Server**（VS Code Remote-SSH、JetBrains Gateway）：体验最全，但远端进程很重；VS Code Server 不开源且许可禁止第三方使用。
- **远端常驻 relay 守护进程**（Zed、Orca）：远端跑一个自带协议的守护进程，终端、git、监听、搜索全部在远端原生执行，断线后远端工作继续存活。代价是远端交付成本高——Orca 因为 relay 依赖 `node-pty` 与 `@parcel/watcher` 两个原生模块，不得不在远端现场 `npm install`，并为此维护远端 Node 探测、原生依赖缓存与修复、安装锁与 GC 等一整套机制。
- **SSH 作为工具**（LiveAgent）：项目仍是本地的，SSH 是终端标签页的一种，Agent 通过一个 `SSHManager` 元工具访问远端。改造成本最低，但模型必须自己区分本地与远端路径，且它的低成本来自「已有终端子系统」这一前提。

Vetta 当前没有终端、没有 Git 面板、没有 LSP、没有端口转发。这既排除了 LiveAgent 路线的成本优势，也意味着不需要为它们付 relay 的代价。

## 决策

采用**本地大脑 + 远端工具**：Agent 循环、模型调用、凭据、会话存储、MCP、插件、技能、审批 UI 全部留在本机主进程；只把接触「项目在哪台机器上」的那一层换成 SSH 实现。

### 项目身份

`ProjectEntry` 的位置字段改为判别联合：

```text
{ kind: "local"; path }  |  { kind: "ssh"; hostId; remotePath }
```

对外统一用规范化 URI `ssh://<hostId>/<abs-path>` 作为 cwd 字符串，使既有的「cwd 即主键」逻辑（会话映射、活动标签 keying）保持不变，只有真正执行 I/O 的边界才解析 URI。项目身份比较改为 `(hostId, path)` 二元组，路径大小写敏感性由该主机的远端平台决定，不再统一按本机规则归一化。

### 执行边界

两条不可协商的规则，适用于所有远程项目代码路径：

1. **不得静默回退到本地执行。** 对远程 cwd 的文件或命令操作，在 SSH provider 缺失、连接断开或任何错误下都必须失败并报错，绝不改为在本机执行。本机存在同名路径时，本地执行会针对完全错误的仓库给出看似成功的答案。
2. **不得断言无法观察的状态。** 进程与连接状态固定为 `live` / `unverifiable` / `exited` 三态，不允许同义词，不允许把 `unverifiable` 并入任何一侧。判定 `exited` 必须有来自远端的正面证据；传输层故障只能产生 `unverifiable`。

### 传输层

以系统 OpenSSH 二进制为主路径，每主机一条 `ControlMaster` 复用连接。理由是 `~/.ssh/config`、Include、ProxyJump、ProxyCommand、ssh-agent、FIDO 安全密钥、GSSAPI、known_hosts 全部由 OpenSSH 原生支持——用户只要 `ssh host` 能通，Vetta 就能通。传输层定义为接口，Windows 或无系统 ssh 的环境后续可补 `ssh2` 实现。

交互式提示（口令、私钥密码、2FA、主机指纹确认）通过自制 askpass 程序接回 Vetta UI：
系统 `ssh` 的这些提示不读 stdin，只执行 `SSH_ASKPASS` 指向的程序。该程序在运行时生成，
经一个带 token 的 Unix domain socket 把提示交给主进程，再由全局浮层问用户。

拿不到回传通道时 askpass 必须以非零退出。若此时静默成功，确认类提示会退化为「默认同意」，
中间人可以静默通过。

### 远端交付

第一阶段远端零安装，全部操作由 `ssh exec` 完成。第二阶段引入远端 helper（`apps/ssh-helper`）：Go 写的静态单文件二进制（`CGO_ENABLED=0`，只用标准库），由本地经 SSH 上传并校验 sha256，不要求远端具备 Node 或任何运行时。四个远端平台（linux/darwin × amd64/arm64）的构建随每个安装包分发——helper 的目标平台与桌面平台无关。

helper 的安装目录与握手版本使用**语义化协议版本号**，不使用构建内容哈希。按构建哈希命名会让每次应用升级后的新客户端连不上仍在运行的旧守护进程，旧会话永久失联且无法在原理上恢复。内容是否需要重传由 sha256 比对决定，目录名不变。

helper **没有守护进程**。后台任务是一个脱离会话的进程组，全部状态落在与版本无关的目录里（`meta.json`、`output.log`、由包装 shell 写下的 `exit`），之后启动的任何一个 helper 进程都能据此列出、续读、终止任务。守护进程会多出一个会崩溃的东西、一个要保护的 socket，以及守护进程与新客户端之间的版本偏差。

helper 是可选加速而不是前提：平台没有构建、校验不过、握手超时、执行器不支持长驻通道时一律降级到 `ssh exec`，并留下诊断日志。它带来的是三件 `ssh exec` 做不到的事——后台任务在本机断开后继续运行并可接管、远端本地比对后推送的文件变更通知、带修订号的单次往返条件写。

### 安全边界

- 主机密钥首次连接必须由用户确认指纹，变更时硬性阻断，不提供 `StrictHostKeyChecking=no` 开关。
- 凭据复用既有 `CredentialVault` 与 `safeStorage`，配置内只存 `credentialRef`，不存明文口令。
- keyboard-interactive / MFA 主机由用户在 UI 完成认证；一次性验证码不进凭据库。口令与私钥密码仅在用户显式勾选「记住」时写入 `CredentialVault`，且同一轮认证被重复追问即判定存档失效并删除——否则错误的存档会让 OpenSSH 把它连试三次，用户只看到卡住然后失败。
- agent forwarding 默认关闭，按主机显式开启并提示风险。
- 本机环境变量不透传远端；远端凭据使用远端自身配置。
- helper 以登录用户身份运行，不提权，仅通过 stdio 通信，不监听 TCP。

## 迁移策略

按行为保持与行为变化分离推进：

1. **前置重构**（行为不变）：项目写入收口到 `ProjectService`；引入 `ProjectLocation`。原计划的「大一统 `WorkspaceFileSystem` 接口」没有采用——各层已有自己的注入点，再叠一层只是为抽象而抽象。实际做法是让每一层的既有端口能声明「在哪台机器上」：
   - 工具：`ToolPathHost`（路径在哪台机器上解析、探测）、`ToolProcessSpawner`（ripgrep / fd 在哪台机器上启动）与原有的 `*Operations`。
   - 资源发现：`ResourceAccessPort` 的一个按路径归属分发的实现，`ssh://` 路径读远端，其余读本机；发现逻辑不改。
   - Desktop 文件预览与媒体协议：把「字节从哪来」拆成小接口（`PreviewFileSource`、媒体来源），呈现判断共用一份。
2. **SSH 可用形态**：`ssh-transport` 连接管理与主机模型；`runtime-ssh` 实现各工具端口；设置页与添加项目入口；文件树的增删改移、预览、`@` 列举、插件的命令与文件能力都按路径归属分流。

   搜索类工具（grep、glob、find、dir_tree）**在远程会话注册**，ripgrep 与 fd 在远端启动。远端没装时工具明说并指出用 bash 里的 grep/find 替代——绝不退回去搜本机磁盘。

   远程会话固定为「完全访问」：沙箱只能约束本机进程，而沙箱模式的核心工具由本机沙箱工具集提供，原样带入会让读写与命令工具整组消失且不报错。会话中途请求沙箱则明确拒绝。
3. **远端 helper**：见「远端交付」。已接入后台任务托管、文件变更通知，以及日常文件操作——helper 就绪后 stat、读、写不再为每一次操作新起一个 ssh 进程。edit 工具的写入带上读取那一刻的内容摘要，由 helper 在远端原子地核对，读与写之间文件被别人改过就拒绝覆盖。内置搜索没有做，搜索仍依赖远端的 ripgrep / fd。

迁移期间不得引入第二套 Agent 执行路径：远程与本地共用同一 Kernel、同一工具逻辑，差异只存在于注入的 `*Operations` 实现。

## 备选方案

- **远端完整 Runtime**：把 Vetta Kernel 搬到远端，可获得「本机关机后远端继续跑」。代价是远端需要 Node 运行时，模型密钥与会话存储落到远端，且需要维护远端安装与升级链路。当前不采用；若日后该能力成为硬需求，需要另立 ADR 重新评估。
- **SSHFS / 网络挂载**：远端进程写入不触发 inotify，而 Agent 恰是远端进程；大仓库逐文件 stat 极慢；macOS 需要 macFUSE。不采用。
- **SSH 作为 Agent 工具**：不改项目模型，仅新增一个远程操作元工具。模型需自行区分本地与远端路径，文件树与编辑器仍指向本地，且 Vetta 缺少让该方案变便宜的终端子系统。不采用。

## 后果

- 项目位置成为显式的一等概念，`kind` 的引入使编译器能定位所有隐含「路径即本地」假设的调用点。
- 模型密钥、会话历史、插件与 MCP 生态不受远程化影响；远端主机被回收后本地会话仍然完整。
- 远端零安装，对共享服务器与受控跳板机友好。
- **本机断开时当前回合中断**：Agent 循环在本机，这是本决策的结构性限制；会话记录留在本地，重连后可续接。后台任务不在此列——远端有 helper 时它们由远端托管，断开后继续运行。前台命令仍绑在 SSH 通道上。
- **无 pty 的通道关闭不会让远端进程结束**：`ssh -T` 下没有控制终端，也就没有 SIGHUP。取消或超时必须显式去远端终止进程组，且只有在该进程组确属本条命令（sshd 已 `setsid`）时才整组终止，否则只杀单个进程——那个组里还有同一连接上的其它命令。
- 本机沙箱（seatbelt / bubblewrap）对远端命令无效，远程会话固定为完全访问并在系统提示词与发布说明中明示。更保守的默认审批策略尚未实现。
- MCP 服务器运行在本机，无法访问远端项目：宿主不向它们通告工作区根，并在系统提示词中说明。插件按能力区分——声明的一次性命令（`command.run`）与文件能力按 cwd 归属改到远端执行，Git 面板因此直接作用于远端仓库；靠本机端口与界面通信的长驻进程（`command.spawn`，如开发服务器）也在远端执行，端口在远端分配再经 `ssh -O forward` 转回本机，插件拿到的始终是一个本机可连的端口号。
- 项目级设置存储是同步读写加文件锁，做不到跨网络：远程项目的项目级设置存放在本机 agent 目录下按项目分片的位置，远端仓库里签入的 `.vetta/settings.json` 不被读取。
- 宿主交给模型的本机路径（粘贴的图片、技能参考资料、截断输出的完整日志）在远程会话里由 read 工具按「本机挂载目录」读取；写入与命令始终作用在远端。
- 每次工具调用增加一次网络往返；`edit` 需要读写两次往返，在高延迟链路上可感知，由第二阶段 helper 合并为一次。
- **端口转发已经实现**（背景一节写「当前没有端口转发」是立项时的事实，此处更新）：它是远程开发的必要一环——界面与浏览器只能连本机端口，而服务必须跑在项目所在的机器上。实现建立在既有的 ControlMaster 上（`ssh -O forward`），不新起常驻 `ssh -L` 进程，因此不必再过一次认证；转发的账本由主进程持有，界面、插件与巡检共用同一份。两点由此决定：转发随 master 一起消失，需要一层巡检把「静默失效」变成界面上的状态，且失效后只自动重建一次——重建可能触发口令提示，循环重试会变成骚扰；远端 sshd 关掉转发（`AllowTcpForwarding no`，精简镜像与堡垒机上常见）时如实报错，不退化成一个连不通的本机端口。转发不持久化：它依附于本进程起的 ssh master，应用一退全部消失。
- 「远端有哪些端口在听」由 helper 读 `/proc`（仅 Linux）或 `ssh exec` 跑 `ss`/`netstat`/`lsof` 回答；三者都没有时如实回答「无从扫描」，界面据此改成让用户手动填端口号，而不是谎称没有端口在听。
- **终端已经实现**（背景一节写「当前没有终端」同样是立项时的事实）：会话页底部面板里的终端在远程项目上直接在远端起交互式 shell，见 [ADR-0125](./0125-session-bottom-panel-and-interactive-terminal.md)。本决策的执行边界判断不变——本地大脑 + 远端工具，远程 cwd 绝不退回本机执行——但边界上多了一条 PTY 通道：helper 新增 `pty.*`（协议 1.1.0），不可用时退回 `ssh -tt`。终端的生命周期与上面那条 `proc.*` 的落盘保活模型刻意相反：pty 随通道结束、输出不落盘，理由记在 ADR-0125。
