# Batch 30 — ModelsProvidersSection + WebhookEndpointList 脱 hold

## 状态

**done**

## 变更

| desktop | theme-ui | 原语 |
|---------|----------|------|
| ModelsProvidersSection | ModelsProvidersSectionView | Button `@vetta/ui` |
| WebhookEndpointList | WebhookEndpointListView | Switch `@vetta/ui` |

desktop 仅 model 适配 / 子表单 slot（ModelsProviderRow 仍可能 hold）。

## 计数

`host_primitive_hold` export ≈ **76**（settings 约 28）
